{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "run",

			// Prints to the debug console instead of the terminal
			// Can't see statics in debugger
			"type": "cppdbg",
			"MIMode": "lldb",

			// Doesn't support expression-based breakpoints
			// Casting in watch window will give "invalid syntax"
			// "type": "lldb",

			"request": "launch",
			"program": "${workspaceFolder}/miniRT",

			// "args": [ "scenes/example.rt" ],

			// "args": [ "scenes/comment.rt" ],
			"args": [ "scenes/comment-on-own-line.rt" ],
			// "args": [ "scenes/empty.rt" ],
			// "args": [ "scenes/extra-field.rt" ],
			// "args": [ "scenes/extra-light.rt" ],
			// "args": [ "scenes/extra-trailing-space.rt" ],
			// "args": [ "scenes/letter-instead-of-float.rt" ],
			// "args": [ "scenes/missing-whitespace.rt" ],
			// "args": [ "scenes/newlines.rt" ],
			// "args": [ "scenes/space-before-object-type.rt" ],

			"cwd": "${workspaceFolder}",
			"preLaunchTask": "Build",
			"internalConsoleOptions": "openOnSessionStart"
		}
	]
}
